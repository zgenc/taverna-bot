{
  "name": "taverna-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc",
    "pm2:flush": "pm2 flush",
    "pm2:delete": "pm2 delete taverna-api || true",
    "pm2:start": "pm2 start dist/index.js --name 'taverna-api'",
    "pm2:logs": "pm2 logs taverna-api",
    "deploy": "npm install && npm run build && npm run pm2:flush && npm run pm2:delete && npm run pm2:start"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zgenc/taverna-bot.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/zgenc/taverna-bot/issues"
  },
  "homepage": "https://github.com/zgenc/taverna-bot#readme",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "better-sqlite3": "^11.0.0",
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2",
    "openai": "^6.22.0",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "*",
    "@types/node": "^20.14.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
